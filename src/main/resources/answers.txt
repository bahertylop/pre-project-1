Вопросы к ревью
1. Что такое Maven? Для чего он нужен? Как добавлять в проект библиотеки без него?

Apache Maven — фреймворк для автоматизации сборки проектов на основе описания их структуры в файлах POM, на языке XML.
maven - система сборки проектов
Вся структура проекта описывается в файле pom.xml (POM – Project Object Model),
который должен находиться в корневой папке проекта.

Зависимости — это те библиотеки, которые непосредственно используются в вашем проекте для компиляции кода или его тестирования.
жизненный цикл:
    clean — удаляются все скомпилированные файлы из каталога target (место, в котором сохраняются готовые артефакты);
    validate — идет проверка, вся ли информация доступна для сборки проекта;
    compile — компилируются файлы с исходным кодом;
    test — запускаются тесты;
    package — упаковываются скомпилированные файлы (в jar, war и т.д. архив);
    verify — выполняются проверки для подтверждения готовности упакованного файла;
    install — пакет помещается в локальный репозиторий. Теперь он может использоваться другими проектами как внешняя библиотека;
    site — создается документация проекта;
    deploy — собранный архив копируется в удаленный репозиторий.

Как добавлять в проект библиотеки без него?
скачать jarник библиотеки, добавить его через project-structure -> libraries



2. Как добавить dependency в Maven? Для чего они нужны? Откуда они скачиваются?

dependency - зависимость - это внешние библиотеки, добавляют новый функционал
Зависимости — это те библиотеки, которые непосредственно используются в вашем проекте для компиляции кода или его тестирования.

maven автоматически подтягивает с центрального репозитория, но также можно с любого другого репозитория
репозиторий указывается через блок <repository>

добавляются в блок <dependencies> пример:
<dependencies>
    <dependency>
        <groupId>org.hibernate</groupId>
        <artifactId>hibernate-core</artifactId>
        <version>6.4.4.Final</version>
    </dependency>
</dependencies>

все зависимости сохраняются в локальный репозиторий: ~/.m2/repository/



3. Основные фазы проекта под управлением Maven?

Зависимости — это те библиотеки, которые непосредственно используются в вашем проекте для компиляции кода или его тестирования.
жизненный цикл:
    clean — удаляются все скомпилированные файлы из каталога target (место, в котором сохраняются готовые артефакты);
    validate — идет проверка, вся ли информация доступна для сборки проекта;
    compile — компилируются файлы с исходным кодом;
    test — запускаются тесты;
    package — упаковываются скомпилированные файлы (в jar, war и т.д. архив);
    verify — выполняются проверки для подтверждения готовности упакованного файла;
    install — пакет помещается в локальный репозиторий. Теперь он может использоваться другими проектами как внешняя библиотека;
    site — создается документация проекта;
    deploy — собранный архив копируется в удаленный репозиторий.



4. Что такое JDBC? Какие классы/интерфейсы относятся к JDBC?

JDBC - Java DataBase Connectivity - API для работы с базами данных на Java
JDBC является частью JDK и предоставляет унифицированный интерфейс для работы с разными СУБД.
предназначен для работы с реляционными базами данных
не зависит от конкретной базы данных
чтобы работать с определенной базой данных под нее пишется драйвер (библиотека для работы с ней)

JDBC API
    Connection - объект отвечает за соединение с базой и режим работы с ней
    Statement - объект предоставляет выражение обращения к базе
    ResultSet - Объект с результатом запроса, который вернула база

Компонент	        Описание
DriverManager	    (класс) Управляет драйверами базы данных, устанавливает соединение.
Connection	        (интерфейс) Соединение с базой данных.
Statement	        (интерфейс) Выполняет SQL-запросы без параметров.
PreparedStatement	(интерфейс) Выполняет SQL-запросы с параметрами (защита от SQL-инъекций).
CallableStatement	(интерфейс) Вызывает хранимые процедуры в базе данных.
ResultSet	        (интерфейс) Хранит результаты SQL-запроса.
SQLException    	(класс) Исключение, возникающее при ошибках работы с БД.



5. Для чего нужен DriverManager?

DriverManager - класс, который управляет драйверами базы данных, устанавливает соединение.
Базовая служба для управления набором драйверов JDBC.

Драйверы подгружаются автоматически из maven



6. Что такое Statement, PreparedStatement, CallableStatement?

интерфейсы для работы с SQL запросами
Statement - интерфейс для работы с запросами без параметров
PreparedStatement - интерфейс для работы с запросами, имеющими параметры защищен от sql инъекций, в отличии от конкатенации строк
CallableStatement - инерфейс для работы с хранимыми процедурами

Statement – простой SQL-запрос
🔹 Используется, когда SQL-запрос не содержит параметров
🔹 Каждый раз пересылается и компилируется сервером базы данных
🔹 Подвержен SQL-инъекциям

PreparedStatement – безопасный и оптимизированный вариант
🔹 Используется, когда в запросе есть параметры (?)
🔹 Предкомпилируется (БД кэширует его, ускоряя выполнение)
🔹 Защищает от SQL-инъекций

CallableStatement – вызов хранимых процедур
🔹 Используется для вызова хранимых процедур в базе данных
🔹 Поддерживает входные (IN) и выходные (OUT) параметры



7. Что такое sql-injection?

sql-injection - sql-инъекции - уязвимость, при которой пользователь может в поле ввода вставить sql запрос,
который выполнится на сервере

SQL-инъекция (SQL Injection)
🔹 SQL-инъекция — это уязвимость в веб-приложениях, когда злоумышленник может вставить вредоносный SQL-код в запрос к базе данных.
🔹 Это может привести к краже данных, удалению таблиц, получению доступа к админке и другим опасным последствиям.

как защищиться:
    PreparedStatement - разделяет запрос и параметры
    Hibernate



8. Что такое ResultSet? Как с ним работать?

ResultSet - интерфейс, для работы с результатами sql запроса, представляет собой таблицу данных
как работать:
    перебор с помошью итератора .next()
    получение значений с помощью геттеров getInt(), getString(), ...

    Метод	                        Описание
rs.next()	                    Переход к следующей строке (возвращает false, если строк больше нет).
rs.getInt("id")	                Получает int из столбца id.
rs.getString(2)	                Получает String из второго столбца.
rs.getDouble("salary")	        Получает double из столбца salary.
rs.wasNull()	                Проверяет, был ли последний столбец NULL.
rs.getDate("created_at")	    Получает Date из столбца created_at.

нужно закрывать или использовать в блоке try c ресурсами



9. Рассказать про паттерн DAO.

DAO - Data Access Object - объект, отделяющий логику работы с базой данных от бизнес-логики
изолирует логику работы с базой данных от бизнес-логики приложения, обеспечивает абстракцию для работы с данными,
что упрощает поддержку и тестирование кода

4 основные операции: CRUD
    Create (создание) — INSERT
    Read (чтение) — SELECT
    Update (обновление) — UPDATE
    Delete (удаление) — DELETE



10. Что такое JPA?

JPA - Java Persistence API - стандарт для работы с базами данных в Java
Jpa - это не реализация, а спецификация, интерфейс для работы с базами данных. Определяет, как должна работать ORM,
для использования требует реализации.
Например:
    Hibernate (самая популярная)
    EclipseLink
    OpenJPA

JPA превращает Java-объекты (классы) в таблицы базы данных с помощью аннотаций

Основные аннотации JPA:
Аннотация	                                            Описание
@Entity	                                                Обозначает класс как сущность (таблицу в БД)
@Table(name = "table_name")	                            Определяет имя таблицы
@Id	                                                    Обозначает первичный ключ
@GeneratedValue(strategy = GenerationType.IDENTITY)	    Автоинкремент ID
@Column(name = "column_name")	                        Определяет колонку в таблице
@OneToOne, @OneToMany, @ManyToOne, @ManyToMany	        Определяют связи между таблицами

Преимущества JPA
    Автоматическое преобразование объектов в SQL
    Не нужно писать сложные SQL-запросы
    Работает с разными базами данных (PostgreSQL, MySQL, H2, и т. д.)
    Spring Data JPA упрощает работу (готовые CRUD-методы)

Минусы
    Требует изучения аннотаций и конфигурации
    Автоматические запросы не всегда оптимальны (иногда нужно писать @Query)



11. Что такое ORM?

ORM - Object Relation Mapping - связь между обхектной моделью программы и реляционной моделью данных
по сути, мапает объекты в строки таблицы и обратно

Подход, который представляет взаимодействие с базой данных в виде взаимодействия с объектами,
предсталяющими таблицы реляционной базы данных

JPA (Java Persistence API) — стандарт ORM
ORM (Object-Relational Mapping, объектно-реляционное отображение) — это технология,
которая позволяет работать с базой данных, используя объекты, а не SQL-запросы.



12. Что такое Hibernate?

Hibernate - фреймворк, который реализует JPA, предоставляет возможность работы с базами данных в Java
Hibernate — это популярная ORM-библиотека для Java, которая реализует спецификацию JPA (Java Persistence API).
Она позволяет разработчикам работать с базой данных, используя объекты Java, а не писать SQL-запросы вручную.
Hibernate выполняет все необходимые операции по сопоставлению объектов с записями в базе данных, а также по
выполнению CRUD (Create, Read, Update, Delete) операций.

Особенности Hibernate:
    ORM (Object-Relational Mapping)
        Hibernate позволяет отображать таблицы базы данных в объекты Java и строки таблиц — в экземпляры этих объектов.
        Это устраняет необходимость в написании SQL-запросов для выполнения операций с данными.
    Автоматическое создание SQL-запросов
        Hibernate генерирует SQL-запросы для операций с базой данных автоматически. Это избавляет разработчика
        от необходимости писать SQL вручную.
    Поддержка транзакций
        Hibernate интегрируется с системами управления транзакциями (например, JTA), что позволяет эффективно управлять
        транзакциями в приложении.
    Кэширование
        Hibernate поддерживает несколько уровней кэширования, что помогает улучшить производительность, особенно при работе
        с большими объемами данных.
    Кросс-БД совместимость
        Hibernate поддерживает несколько баз данных, таких как MySQL, PostgreSQL, Oracle, SQL Server и другие.
        Это позволяет легко переключать СУБД без необходимости переписывать код.
    Поддержка связей между сущностями
        Hibernate поддерживает работу с связями один к одному, один ко многим, многие ко многим и многие к одному.
        Это удобно для работы с более сложными структурами данных.

Основные компоненты Hibernate
    SessionFactory — Фабрика для создания Session. Это основной компонент, который настраивает Hibernate.
    Session — Основной интерфейс для работы с данными (выполнение запросов, сохранение и удаление объектов).
    Transaction — Интерфейс для работы с транзакциями.
    Query — Интерфейс для выполнения запросов к базе данных.
    Criteria API — Позволяет создавать запросы с использованием объекта Criteria (для гибкости).



13. В чем разница между JPA и Hibernate? Как связаны все эти понятия?

JPA - это спецификация, Hibernate - одна из реализаций JPA, добавляет доп фичи, HQL, кэширование второго уровня,
расширенные возможности маппинга



14. Какие классы/интерфейсы относятся к JPA/Hibernate?

Интерфейсы и классы JPA:
    EntityManager
    Описание: Это основной интерфейс для работы с объектами персистентного контекста. Он управляет жизненным циклом
    объектов и обеспечивает операции, такие как создание, удаление и обновление сущностей, а также выполнение запросов.
    Методы:
    persist(T entity): сохраняет сущность.
    merge(T entity): объединяет сущность.
    remove(T entity): удаляет сущность.
    find(Class<T> entityClass, Object primaryKey): находит сущность по ключу.
    createQuery(String qlString): создает запрос JPQL.

    EntityTransaction
    Описание: Интерфейс для работы с транзакциями в JPA. Он позволяет начинать, коммитить и откатывать транзакции.
    Методы:
    begin(): начинает транзакцию.
    commit(): коммитит транзакцию.
    rollback(): откатывает транзакцию.

    Query
    Описание: Интерфейс для работы с запросами в JPA. Он позволяет выполнять запросы, получая результаты.
    Методы:
    getResultList(): выполняет запрос и возвращает список результатов.
    getSingleResult(): выполняет запрос и возвращает единственный результат.

    TypedQuery
    Описание: Это расширение интерфейса Query, которое позволяет работать с типизированными результатами.
    Методы:
    setParameter(String name, Object value): устанавливает параметр в запросе.
    getResultList(): возвращает результат в виде списка объектов заданного типа.

    CriteriaBuilder
    Описание: Интерфейс для построения динамических запросов в JPA, используя API Criteria
    (например, для создания запросов с фильтрацией без использования строковых запросов).
    Методы:
    createQuery(Class<T> resultClass): создает запрос для сущностей.
    equal(Expression<?> x, Object y): создает условие для сравнения.

    CriteriaQuery
    Описание: Интерфейс для представления самого запроса, который можно будет выполнить.
    Методы:
    select(Expression<T> selection): задает, какие данные будут выбраны.
    where(Predicate... restrictions): задает ограничения запроса.

    Persistence
    Описание: Это основной класс для работы с фабрикой сущностей. Он предоставляет методы для
    создания экземпляра EntityManagerFactory.
    Методы:
    createEntityManagerFactory(String persistenceUnitName): создает фабрику для EntityManager.

Интерфейсы и классы Hibernate:
    Session
    Описание: Этот интерфейс представляет собой контекст для работы с объектами Hibernate,
    аналог EntityManager в JPA. Он является основным интерфейсом для взаимодействия с базой данных.
    Методы:
    save(Object entity): сохраняет сущность.
    update(Object entity): обновляет сущность.
    delete(Object entity): удаляет сущность.
    createQuery(String hql): создает HQL-запрос.
    beginTransaction(): начинает транзакцию.
    get(Class<T> clazz, Serializable id): находит объект по ID.

    SessionFactory
    Описание: Это фабрика для создания сессий в Hibernate. Она настраивает соединение с базой данных и управляет кэшированием.
    Методы:
    openSession(): открывает новую сессию.
    getCurrentSession(): возвращает текущую сессию (в контексте транзакции).
    close(): закрывает фабрику сессий.

    Transaction
    Описание: Интерфейс, который представляет собой транзакцию в Hibernate. Он аналогичен EntityTransaction в JPA,
    но с дополнительными функциями, такими как управление кэшированием.
    Методы:
    begin(): начинается транзакция.
    commit(): фиксирует транзакцию.
    rollback(): откатывает транзакцию.

    Query
    Описание: Интерфейс для выполнения запросов в Hibernate, аналогичный Query в JPA, но с расширением возможностей.
    Hibernate использует HQL (Hibernate Query Language), который напоминает SQL, но работает с объектами.
    Методы:
    list(): выполняет запрос и возвращает список результатов.
    uniqueResult(): выполняет запрос и возвращает единственный результат.

    Criteria (устаревший)
    Описание: Это интерфейс для создания запросов на основе критериев в Hibernate. В новых версиях Hibernate
    рекомендуется использовать CriteriaQuery и CriteriaBuilder JPA.
    Методы:
    add(Restriction restriction): добавляет ограничение к запросу.
    list(): выполняет запрос и возвращает результаты.

    HQL (Hibernate Query Language)
    Описание: Это язык запросов, предоставляемый Hibernate для работы с объектами вместо таблиц базы данных.
    HQL очень похож на SQL, но работает с сущностями, а не с таблицами.

    Configuration
    Описание: Класс для конфигурации Hibernate, который настраивает параметры подключения и маппинг сущностей.
    Методы:
    configure(String configFile): загружает конфигурацию из XML-файла.
    addAnnotatedClass(Class clazz): добавляет аннотированную сущность в конфигурацию.

    Interceptor
    Описание: Это интерфейс, который позволяет перехватывать события Hibernate, такие как сохранение,
    обновление и удаление объектов, а также запросы к базе данных.
    Методы:
    onSave(): перехватывает сохранение сущности.
    onFlushDirty(): перехватывает изменение сущности.

В проекте на Hibernate можно использовать только JPA-интерфейсы, но для использования специфических возможностей
Hibernate потребуется использовать дополнительные классы и интерфейсы, такие как Session, Transaction, и Criteria.



15. Основные аннотации Hibernate, рассказать.

hibernate дергает аннотации JPA

JPA аннотации:

@Entity
@Table
@Id
@Column
@ManyToOne, @OneToMany, @ManyToMany
@Embedded, @Embeddable
@Version
@JoinColumn
Hibernate аннотации (специфичные для Hibernate):

@DynamicInsert  Указывает, что Hibernate будет генерировать SQL запросы с динамическим добавлением только тех полей,
которые изменены (не все поля сущности). Полезно для повышения производительности при работе с большими таблицами.

@DynamicUpdate Указывает Hibernate генерировать SQL запросы для обновления только тех колонок, которые были изменены,
вместо обновления всех колонок. Это может снизить нагрузку на базу данных.

@BatchSize казывает размер батча для пакетной обработки коллекций и ассоциаций. Это может помочь повысить
производительность, например, при загрузке больших коллекций.

@Cache лючает кэширование для сущности, коллекции или запроса. Это позволяет повысить производительность
за счет использования кэша второго уровня.

@NaturalId Описание: Используется для полей, которые имеют естественный (неконфликтующий) уникальный идентификатор.
Этот идентификатор используется для поиска объектов.

@Type  Используется для указания пользовательского типа данных в Hibernate. Например, для сериализации сложных объектов.



16. Чем HQL отличается от SQL?

SQL работает с таблицами, HQL работает с классами,
работая с HQL можно ничего не знать о реальной таблице в базе данных

HQL (Hibernate Query Language) и SQL (Structured Query Language) — это два различных языка запросов, которые служат
для работы с данными в реляционных базах данных, но они имеют несколько важных различий.

1. Уровень абстракции
HQL: Это объектно-ориентированный язык запросов, предназначенный для работы с объектами Java, а не непосредственно
с таблицами в базе данных. HQL использует классы и их свойства, а не таблицы и колонки. Он позволяет работать
с объектами (сущностями) и их связями.

SQL: Это язык запросов, используемый непосредственно для работы с реляционными базами данных, с таблицами,
строками и колонками. SQL работает с данными на уровне базы данных, независимо от объекта или модели.

2. Отличия по синтаксису

3. Работа с объектами и связями
Работа в HQL с join легче, т.к. hibernate шарит за отношения
HQL:
SELECT p FROM Person p JOIN p.addresses a WHERE a.city = 'New York'

SQL:
SELECT p.name FROM Person p JOIN Address a ON p.id = a.person_id WHERE a.city = 'New York'

4. Различные типы данных

5. Поддержка наследования
HQL: HQL поддерживает объектно-ориентированное наследование, позволяя делать запросы к суперклассам или подклассам.
Вы можете запросить объекты всех типов в иерархии, а также использовать полиморфизм

SQL: Для работы с наследованием в SQL необходимо использовать таблицы с различными подходами, например,
"table-per-class" или "table-per-hierarchy". Это требует дополнительных усилий и не имеет такой гибкости, как HQL.

6. Использование базы данных
HQL: HQL выполняет запросы через Hibernate и использует абстракцию для работы с базой данных. Hibernate сам преобразует
HQL в SQL, который будет выполняться в базе данных. HQL позволяет работать с объектами, а не с таблицами и колонками.

SQL: SQL — это язык, который напрямую работает с базой данных, и запросы выполняются в контексте реляционной базы данных.



17. Что такое Query? Как передать в объект Query параметры?

Query — это объект в Hibernate, который представляет собой запрос, который будет выполнен в базе данных.
С помощью Query можно выполнить как HQL (Hibernate Query Language), так и SQL запросы,
используя объектно-ориентированный подход в Hibernate. В зависимости от того, какой тип запроса используется,
синтаксис будет отличаться, но концепция остается одинаковой.

Поддерживает как SQL, так и HQL синтаксис



18. Какие можно устанавливать параметры в hbm2ddl, рассказать про каждый из них.

параметр hbm2ddl - это инструмент в Hibernate, который используется для валидации, автоматического создания, обновления
или удаления схемы базы данных на основе сущностей. Он управляется через настройки в hibernate.cfg.xml или через свойства
в конфигурации. В Hibernate можно настроить параметры для hbm2ddl с помощью свойства hibernate.hbm2ddl.auto.

возможные значения:
    none - базовое значение, ничго не делает
    create - удаляет существующие таблицы, создает новые на основе сущностей
    create-drop - удаляет существующие таблицы, создает новые на основе сущностей, снова удаляет при завершении работы приложения
    update - Hibernate проверяет текущую схему и применяет изменения, чтобы она соответствовала текущим сущностям.
    validate - Hibernate проверяет, что схема базы данных соответствует сущностям, но не вносит изменений. Выбросит исключение,
    если схемы не совпадают с сущностями

    hibernate.hbm2ddl.import_files файлы, выполнятся при create
    hibernate.hbm2ddl.import_files_sql_extractor Этот параметр позволяет указать собственный механизм для извлечения SQL-скриптов из файлов.
    hibernate.hbm2ddl.delimiter указывает разделитель между sql запросами в файлах
    hibernate.hbm2ddl.generate_statistics Этот параметр позволяет включить или отключить сбор статистики для операций с hbm2ddl.



19. Требования JPA к Entity-классам? Не менее пяти.

1. Наличие аннотации @Entity
2. Наличие @Id
3. Конструктор с параметрами?
4. Конструктор без параметров
5. @Table(name), если имя таблицы не совпадает с именем класса сущности
6. @GeneratedValue для автоматической генерации ключа (необязательно)
7. Поле, соответствующее столбцу, должно быть совместимо с типом данных базы данных:



20. Жизненный цикл Entity в Hibernate? Рассказать.

1. Transient (Временная сущность) - при создании сущности через конструктор, не переходит в контекст
Сущность находится в состоянии transient (временная сущность), если она была создана как объект Java,
но еще не была сохранена в базе данных.

Такие объекты не имеют связи с сессией Hibernate и не отслеживаются. Если они не сохраняются в базе данных,
они исчезают после завершения работы программы.

2. Persistent (Сущность в контексте сессии) - переходит в это состояние, когда сохраняется в базе данных
Сущность переходит в состояние persistent (постоянная сущность), когда она сохраняется в базе данных через
Session.save(), Session.persist(), Session.merge() или загружается с помощью Session.load() или Session.get().

В этом состоянии объект привязан к текущей сессии, и изменения в объекте автоматически отслеживаются и синхронизируются
с базой данных при коммите транзакции.

3. Detached (Отключенная сущность) - когда сессия закончилась, или удален из контекста сессии
Сущность становится detached (отключенной), если она была отсоединена от сессии Hibernate.
Это может произойти по нескольким причинам, например:
    Когда сессия была закрыта (сущность больше не отслеживается).
    Когда объект был удален из контекста сессии с помощью Session.evict().
    Когда сессия была завершена или закрыта.

В этом состоянии объект больше не отслеживается Hibernate, и изменения, внесенные в объект,
не синхронизируются с базой данных, пока объект снова не будет привязан к сессии.

4. Removed (Удаленная сущность) - удаленный из базы данных объект

Сущность становится removed (удаленной), когда она была удалена из базы данных с помощью Session.delete().
После выполнения операции удаления сущность будет помечена как удаленная, и изменения будут синхронизированы
с базой данных после коммита транзакции.


Порядок перехода между состояниями:
Transient → Persistent: Когда сущность сохраняется в базе данных с помощью save(), persist() или когда она загружается
с базы через get() или load().
Persistent → Detached: Когда сессия закрывается или объект извлекается из контекста сессии.
Persistent → Removed: Когда сущность удаляется из базы данных с помощью delete().
Detached → Persistent: Когда отключенная сущность снова присоединяется к сессии через update(), merge() или saveOrUpdate().
Removed → Deleted: Когда сущность была фактически удалена из базы данных.